Fix cold-start state machine and landing HeroMark layout after real-user browser test.

- app/api/wave/route.ts: commit SESSION_STARTED when a guest session is first created by GET /api/wave, so the XState ledger starts from 'entry' correctly even when the user lands on /play before calling /api/session.
- app/api/session/route.ts: also ensure SESSION_STARTED is committed when the session has no persisted snapshot, preventing state desync when the consent flow begins.
- components/landing/HeroMark.tsx: move card transforms into motion animate props (x/y/rotate) instead of inline style, so the fanned three-card composition renders correctly; add z-index layering and slightly wider spread.

Verification: typecheck, build, lint (0 errors), test:contracts (31/31), and Playwright e2e+integration (48/48) all pass.