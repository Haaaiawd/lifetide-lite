Fix final-phase reload, idempotent final plan, and make landing a conversation.

- app/api/wave/route.ts: GET now returns stop when the XState ledger is in parallel_lives_ready, trial_active, or bounded_reflection, so /play reload no longer falls back to a new wave.
- app/api/final/route.ts: stores the generated FinalPlan in WorkingMemory and returns it on replay, making "生成三条路线" idempotent.
- lib/working-memory/types.ts & schema.ts: add optional finalPlan field to WorkingMemory.
- components/landing/LandingPage.tsx: replace hero/sample with a chat-style thread that asks for the user's current state, shows a choice card, and then reveals the "开始试运行" CTA.
- tests/e2e/visual-prototype.spec.ts & screenshot-flow.spec.ts: update landing selectors to match the new conversation UI.

Verification: typecheck, build, lint (0 errors), test:contracts (31/31), test:e2e (48/48), and a real-user agent-browser run through landing → consent → Wave 1 → insight → routes → reload → regenerate all pass.